{
  "name": "@sismo-core/sismo-libs",
  "version": "1.0.0",
  "description": "Utils package used in the Sismo Protocol",
  "main": "index.js",
  "repository": "git@github.com:sismo-core/sismo-utils.git",
  "author": "Sismo core",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "jest",
    "build": "yarn && yarn build:crypto && yarn build:kv-merkle-tree",
    "build:crypto": "cd packages/crypto && yarn && yarn build",
    "build:kv-merkle-tree": "cd packages/kv-merkle-tree && yarn && yarn build"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.22",
    "ethers": "^5.6.1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.4",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {},
  "npmClient": "yarn",
  "engines": {
    "npm": "NPM IS NOT USED - use yarn instead."
  }
}
